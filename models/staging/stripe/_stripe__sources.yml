version: 2

sources:
  - name: stripe
    database: raw  
    schema: stripe  
    tables:
      - name: payment
        columns:
          - name: id
            tests:
              - not_null
              - unique
        loaded_at_field: _batched_at
        freshness: 
          warn_after:
            count: 10
            period: day
          error_after:
            count: 20
            period: day
        
